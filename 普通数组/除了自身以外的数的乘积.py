# 这道题禁止用除法，要求 O (n) 时间
from typing import List

# 非进阶版，用左右各一个数组
def getMulti1(nums: List[int]) -> List[int]:
    n = len(nums)

    #
    res = [1]*n
    left = [1]*n
    right = [1]*n

    for i in range(n):
        left

    return res